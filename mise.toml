[settings]
experimental = true  # turn on support for `mise deps`
idiomatic_version_file_enable_tools = ["python"]  # use .python-version
install_before = "7d"  # only resolve to versions released more than 7 days ago
lockfile = true  # turn on lockfile support
locked = true  # fail if we try to install something not in the lockfile
locked_verify_provenance = true  # verify provenance even with a lockfile.

[tools]
make = "latest"
uv = "latest"

[env]
_.python.venv = { path = ".venv", create = true }

[deps.pip]
auto = true
sources = [
    "requirements/deploy.txt",
    "requirements/main.txt",
    "requirements/dev.txt",
    "requirements/tests.txt",
    "requirements/lint.txt"
]
# Use `uv pip` instead of just `pip`
run = "uv pip install -r requirements/deploy.txt -r requirements/main.txt -r requirements/dev.txt -r requirements/tests.txt -r requirements/lint.txt"

[tasks.fmt]
run = ["./bin/reformat"]

[tasks.lint]
run = ["./bin/lint"]

[tasks.licenses]
run = ["./bin/licenses"]

[tasks.translations]
run = ["./bin/translations"]
